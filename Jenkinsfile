// Common pipeline functions (from Groovy)
def common = null;

/* Requires the Docker Pipeline plugin */
pipeline {
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}
